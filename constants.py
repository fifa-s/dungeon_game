"""All major constants that affect the whole game should be defined here"""

SCREEN_SIZE = (1920, 1080)
DISPLAY_SIZE = (1920, 1080) # Not so useful, just the size the screen resizes to when you minimize the window